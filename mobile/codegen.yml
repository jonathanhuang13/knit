schema: ${GRAPHQL_ENDPOINT:http://localhost:4001}
documents: './src/**/*.graphql'
generates:
  ./src/graphql/generated.ts:
    plugins:
      - typescript
      - typescript-operations
      - typed-document-node
